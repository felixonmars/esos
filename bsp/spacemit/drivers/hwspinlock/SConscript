# for module compiling
import os
from building import *

cwd  = GetCurrentDir()
src     = Glob('*.c')
CPPPATH = [cwd]

group = DefineGroup('Drivers_hwspinlock', src, depend = ['BSP_USING_HWSPINLOCK'], CPPPATH = CPPPATH)

Return('group')
