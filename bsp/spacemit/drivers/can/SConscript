# for module compiling
import os
from building import *

cwd  = GetCurrentDir()
src     = Glob('*.c')
CPPPATH = [cwd]

group = DefineGroup('Drivers_adma', src, depend = ['BSP_USING_CAN'], CPPPATH = CPPPATH)

Return('group')
